# from subject import LMConfig
# from agrabber import ActivationGrabber
from subject import Subject


if __name__ == "__main__":
    print("Hello, world!")
    # subject = Subject(LMConfig())
    # sub_gen = subject.generate("Hello, world!")
    # for token_id in sub_gen:
    #     print(subject.decode(token_id), end="", flush=True)
    # grabber = ActivationGrabber(subject)
    # print(grabber.get_activations("Hello, world!"))
        
    
